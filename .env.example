# Ollama base URL — override if running on a non-default port
NEXT_PUBLIC_OLLAMA_URL=http://localhost:11434

# OpenRouter API key is set via the in-app settings modal, not here
# Get your key at: https://openrouter.ai/keys
